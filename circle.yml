---
machine:
  post:
    - npm install -g yarn

dependencies:
  override:
    - yarn install

test:
  pre:
    - npm run lint
    - npm run build

deployment:
  npm:
    branch: master
    commands:
      - echo -e "$NPM_USER\n$NPM_PASS\n$NPM_EMAIL" | npm login
      - npm run publish
