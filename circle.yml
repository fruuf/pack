---
test:
  pre:
    - npm run lint
    - npm run build

# dependencies:
#   pre:
#     - npm install --dev
#   override:
#     - npm install

deployment:
  npm:
    branch: master
    commands:
      - echo -e "$NPM_USER\n$NPM_PASS\n$NPM_EMAIL" | npm login
      - npm run publish
